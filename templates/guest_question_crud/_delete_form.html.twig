<form method="post" action="{{ path('app_guest_question_crud_delete', {'id': guest_question.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guest_question.id) }}">
    <button class="btn">Delete</button>
</form>
