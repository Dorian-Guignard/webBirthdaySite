{% extends 'base.html.twig' %}

{% block title %}GuestQuestion{% endblock %}

{% block body %}
    <h1>GuestQuestion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ guest_question.id }}</td>
            </tr>
            <tr>
                <th>Pax</th>
                <td>{{ guest_question.pax }}</td>
            </tr>
            <tr>
                <th>Night</th>
                <td>{{ guest_question.night }}</td>
            </tr>
            <tr>
                <th>Allergie</th>
                <td>{{ guest_question.allergie }}</td>
            </tr>
            <tr>
                <th>Regime</th>
                <td>{{ guest_question.regime }}</td>
            </tr>
            <tr>
                <th>Playlist</th>
                <td>{{ guest_question.playlist }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_guest_question_crud_index') }}">back to list</a>

    <a href="{{ path('app_guest_question_crud_edit', {'id': guest_question.id}) }}">edit</a>

    {{ include('guest_question_crud/_delete_form.html.twig') }}
{% endblock %}
